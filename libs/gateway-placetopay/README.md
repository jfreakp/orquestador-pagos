# gateway-placetopay

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gateway-placetopay` to build the library.

## Running unit tests

Run `nx test gateway-placetopay` to execute the unit tests via [Jest](https://jestjs.io).
