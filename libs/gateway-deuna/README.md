# gateway-deuna

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gateway-deuna` to build the library.

## Running unit tests

Run `nx test gateway-deuna` to execute the unit tests via [Jest](https://jestjs.io).
